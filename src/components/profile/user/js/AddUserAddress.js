import React, { Component } from "react";
import { connect } from "react-redux";

class AddUserAddress extends Component {
  state = {};
  render() {
    return <React.Fragment></React.Fragment>;
  }
}

export default connect(AddUserAddress);
